/* ==========================================================================
   RESET Y CONFIGURACIÓN GENERAL (Accesibilidad y Diseño Armónico)
   ========================================================================== */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

:root {
    /* Paleta de Colores cálida que transmite comida y sabor (Excelente Contraste) */
    --color-primary: #FF9F1C;      /* Naranja brillante apetitoso */
    --color-dark: #1E1B18;         /* Fondo oscuro de alta legibilidad */
    --color-light: #FDF7F2;        /* Fondo claro hueso */
    --color-white: #FFFFFF;
    --color-gray: #4A4540;
    --color-accent: #E71D36;       /* Rojo promociones */
    --font-main: 'Poppins', sans-serif;
    --font-heading: 'Bebas Neue', sans-serif;
}

html {
    scroll-behavior: smooth;
    font-size: 16px;
}

body {
    font-family: var(--font-main);
    background-color: var(--color-light);
    color: var(--color-dark);
    line-height: 1.6;
}

.container {
    width: 90%;
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem 0;
}

/* ==========================================================================
   HEADER / NAVEGACIÓN
   ========================================================================== */
.main-header {
    background-color: var(--color-dark);
    position: sticky;
    top: 0;
    z-index: 1000;
    box-shadow: 0 4px 10px rgba(0,0,0,0.3);
}

.header-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem 0;
}

.logo {
    font-family: var(--font-heading);
    font-size: 2rem;
    color: var(--color-white);
    text-decoration: none;
    letter-spacing: 1px;
}

.logo span {
    color: var(--color-primary);
}

.nav-menu {
    display: flex;
    gap: 1.5rem;
}

.nav-menu a {
    color: var(--color-white);
    text-decoration: none;
    font-weight: 600;
    font-size: 0.95rem;
    transition: color 0.3s;
}

.nav-menu a:hover {
    color: var(--color-primary);
}

/* Botones y CTA */
.btn-cta {
    background-color: var(--color-primary);
    color: var(--color-dark);
    padding: 0.6rem 1.5rem;
    border-radius: 30px;
    font-weight: 700;
    text-decoration: none;
    transition: background-color 0.3s, transform 0.2s;
}

.btn-cta:hover {
    background-color: #e08b12;
    transform: scale(1.05);
}

/* ==========================================================================
   HERO SECTION
   ========================================================================== */
.hero-section {
    background-color: var(--color-dark);
    color: var(--color-white);
    padding: 4rem 0;
}

.hero-container {
    display: flex;
    align-items: center;
    gap: 3rem;
    flex-wrap: wrap;
}

.hero-content {
    flex: 1;
    min-width: 300px;
}

.hero-content h1 {
    font-family: var(--font-heading);
    font-size: 3.8rem;
    line-height: 1.1;
    margin-bottom: 1.5rem;
    color: var(--color-primary);
}

.hero-content p {
    font-size: 1.1rem;
    margin-bottom: 2rem;
    color: #e0dcd3;
}

.hero-buttons {
    display: flex;
    gap: 1rem;
}

.btn-primary {
    background-color: var(--color-primary);
    color: var(--color-dark);
    padding: 0.8rem 2rem;
    border-radius: 8px;
    font-weight: 700;
    text-decoration: none;
    display: inline-block;
    transition: 0.3s;
    border: none;
    cursor: pointer;
}

.btn-primary:hover {
    background-color: #e08b12;
}

.btn-secondary {
    background-color: transparent;
    color: var(--color-white);
    border: 2px solid var(--color-white);
    padding: 0.8rem 2rem;
    border-radius: 8px;
    font-weight: 700;
    text-decoration: none;
    display: inline-block;
    transition: 0.3s;
}

.btn-secondary:hover {
    background-color: var(--color-white);
    color: var(--color-dark);
}

.hero-image {
    flex: 1;
    min-width: 300px;
    text-align: center;
}

.hero-image img {
    max-width: 100%;
    border-radius: 15px;
    box-shadow: 0 10px 30px rgba(0,0,0,0.5);
}

/* ==========================================================================
   ELEMENTOS REUTILIZABLES (Secciones y Tarjetas)
   ========================================================================== */
.section-title {
    font-family: var(--font-heading);
    font-size: 2.8rem;
    text-align: center;
    color: var(--color-dark);
    margin-bottom: 0.5rem;
}

.section-subtitle {
    text-align: center;
    color: var(--color-gray);
    margin-bottom: 3rem;
    font-size: 1.1rem;
}

/* Rejilla de tarjetas (Responsive sin dependencias) */
.grid-cards {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
}

.card {
    background-color: var(--color-white);
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 4px 15px rgba(0,0,0,0.05);
    transition: transform 0.3s;
}

.card:hover {
    transform: translateY(-5px);
}

.card img {
    width: 100%;
    height: 220px;
    object-fit: cover;
}

.card-body {
    padding: 1.5rem;
    text-align: center;
}

.card-body h3 {
    margin-bottom: 0.5rem;
    font-size: 1.3rem;
}

.card-body p {
    color: var(--color-gray);
    font-size: 0.9rem;
    margin-bottom: 1rem;
    height: 60px;
    overflow: hidden;
}

.price {
    display: block;
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-accent);
    margin-bottom: 1rem;
}

.btn-card {
    display: block;
    background-color: var(--color-dark);
    color: var(--color-white);
    padding: 0.7rem 1rem;
    text-decoration: none;
    font-weight: 600;
    border-radius: 6px;
    transition: 0.3s;
}

.btn-card:hover {
    background-color: var(--color-primary);
    color: var(--color-dark);
}

/* ==========================================================================
   SECCIÓN PROMOCIONES (HU02)
   ========================================================================== */
.promos-section {
    background-color: #FFFDF9;
}

.promo-card {
    position: relative;
    border: 2px solid var(--color-primary);
}

.badge {
    position: absolute;
    top: 10px;
    left: 10px;
    background-color: var(--color-accent);
    color: var(--color-white);
    padding: 0.4rem 1rem;
    font-weight: 700;
    font-size: 0.8rem;
    border-radius: 20px;
}

.old-price {
    text-decoration: line-through;
    color: #999;
    font-size: 1.1rem;
    margin-right: 0.5rem;
}

.prices {
    margin-bottom: 1rem;
}

/* ==========================================================================
   TESTIMONIALS
   ========================================================================== */
.testimonials-section {
    background-color: var(--color-dark);
    color: var(--color-white);
    padding: 4rem 0;
}

.testimonials-section .section-title {
    color: var(--color-white);
}

.grid-testimonials {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
    margin-top: 2rem;
}

.testimonial-card {
    background-color: #2e2a25;
    padding: 2rem;
    border-radius: 8px;
    text-align: center;
}

.stars {
    color: var(--color-primary);
    margin-bottom: 1rem;
}

.comment {
    font-style: italic;
    margin-bottom: 1rem;
}

.author {
    font-weight: 700;
    color: var(--color-primary);
}

/* ==========================================================================
   FORMULARIO EVENTOS CORPORATIVOS (HU03)
   ========================================================================== */
.contact-section {
    background-color: var(--color-white);
}

.contact-container {
    display: flex;
    gap: 3rem;
    flex-wrap: wrap;
    align-items: center;
}

.contact-info {
    flex: 1;
    min-width: 300px;
}

.contact-info h2 {
    font-family: var(--font-heading);
    font-size: 2.8rem;
    margin-bottom: 1rem;
}

.info-block {
    margin-top: 1.5rem;
}

.contact-form {
    flex: 1;
    min-width: 300px;
    background-color: var(--color-light);
    padding: 2.5rem;
    border-radius: 12px;
    box-shadow: 0 4px 20px rgba(0,0,0,0.05);
}

.contact-form h3 {
    margin-bottom: 1.5rem;
    font-size: 1.5rem;
}

.form-group {
    margin-bottom: 1.2rem;
}

.form-group label {
    display: block;
    margin-bottom: 0.4rem;
    font-weight: 600;
    font-size: 0.9rem;
}

.form-group input, .form-group textarea {
    width: 100%;
    padding: 0.8rem;
    border: 1px solid #ddd;
    border-radius: 6px;
    font-family: var(--font-main);
    font-size: 0.95rem;
}

.btn-block {
    width: 100%;
}

.form-feedback {
    margin-top: 1rem;
    padding: 0.8rem;
    border-radius: 6px;
    font-weight: 600;
    text-align: center;
}

.form-feedback.success {
    background-color: #d4edda;
    color: #155724;
}

.form-feedback.error {
    background-color: #f8d7da;
    color: #721c24;
}

.hidden {
    display: none;
}

/* ==========================================================================
   UBICACIÓN (HU04)
   ========================================================================== */
.map-placeholder {
    background-color: #ddd;
    height: 300px;
    border-radius: 12px;
    display: flex;
    justify-content: center;
    align-items: center;
    background-image: linear-gradient(rgba(255,159,28,0.1), rgba(0,0,0,0.4)), url('https://images.unsplash.com/photo-1524661135-423995f22d0b?auto=format&fit=crop&w=1200&q=80');
    background-size: cover;
    background-position: center;
}

.map-box {
    background-color: var(--color-white);
    padding: 2rem;
    border-radius: 8px;
    text-align: center;
    box-shadow: 0 4px 15px rgba(0,0,0,0.2);
}

.map-box p {
    font-weight: 700;
}

/* ==========================================================================
   FOOTER
   ========================================================================== */
.main-footer {
    background-color: var(--color-dark);
    color: var(--color-white);
    padding: 3rem 0 1.5rem 0;
}

.footer-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 2rem;
    padding-bottom: 2rem;
    border-bottom: 1px solid #333;
}

.footer-col h4 {
    color: var(--color-primary);
    margin-bottom: 1rem;
}

.footer-col a {
    display: block;
    color: #ccc;
    text-decoration: none;
    margin-bottom: 0.5rem;
}

.footer-col a:hover {
    color: var(--color-primary);
}

.footer-bottom {
    text-align: center;
    padding-top: 1.5rem;
    font-size: 0.85rem;
    color: #888;
}

/* ==========================================================================
   DISEÑO RESPONSIVE (MEDIA QUERIES)
   ========================================================================== */
@media (max-width: 768px) {
    .header-container {
        flex-direction: column;
        gap: 1rem;
    }
    
    .nav-menu {
        flex-wrap: wrap;
        justify-content: center;
        gap: 1rem;
    }

    .hero-content h1 {
        font-size: 2.8rem;
    }

    .hero-container {
        text-align: center;
        flex-direction: column-reverse;
    }

    .hero-buttons {
        justify-content: center;
    }
}